/**
 * Icon set — thin aliases over lucide-react.
 *
 * We standardize on the lucide icon library. DO NOT hand-write SVG paths here.
 * To add an icon, alias the matching lucide export below (browse names at
 * https://lucide.dev), or import it directly from "lucide-react" at the call
 * site. Every alias is a real lucide component, so it accepts the usual props
 * (className, size, strokeWidth, color, aria-*).
 */
export {
  AlertCircle as IconAlertCircle,
  ArrowLeft as IconArrowLeft,
  ArrowRight as IconArrowRight,
  Camera as IconCamera,
  Check as IconCheck,
  ChevronRight as IconChevronRight,
  ChevronDown as IconChevronDown,
  Clock as IconClock,
  LayoutDashboard as IconDashboard,
  FileText as IconDoc,
  Download as IconDownload,
  Pencil as IconEdit,
  Eye as IconEye,
  EyeOff as IconEyeOff,
  CircleHelp as IconHelp,
  House as IconHome,
  Info as IconInfo,
  Keyboard as IconKeyboard,
  LoaderCircle as IconLoader,
  MessageSquare as IconMessageSquare,
  Mic as IconMic,
  Moon as IconMoon,
  Pause as IconPause,
  Play as IconPlay,
  Printer as IconPrinter,
  Repeat as IconRepeat,
  Search as IconSearch,
  Settings as IconSettings,
  Share2 as IconShare,
  Shield as IconShield,
  SkipForward as IconSkip,
  Sparkles as IconSparkle,
  Volume2 as IconSpeaker,
  Square as IconStop,
  Sun as IconSun,
  Trash2 as IconTrash,
  Upload as IconUpload,
  User as IconUser,
  AudioLines as IconWave,
  X as IconX,
  RotateCcw as IconRefresh,
} from "lucide-react";
